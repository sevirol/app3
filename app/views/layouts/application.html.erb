<!DOCTYPE html>
<html>
<head>
  <title>Потрещим?</title>
  <%= render 'layouts/stylesheets' %>
</head>
<body>
	<div class="container">
      <%= render 'layouts/header' %>
      <section class="round">
        <%= yield %>
      </section>
    </div>
</body>
</html>
